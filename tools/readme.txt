If you have issues building, make sure you have libsdl2-dev installed (using apt) and have run chmod +x on BuildDeps.sh (so the build scripts can call it properly)
also install the following for pistache:
libgtest-dev
libssl-dev
libcurl4-openssl-dev
rapidjson-dev



